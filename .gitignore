node_modules/
dist/
*.log
.DS_Store
.env
.env.local
*.tsbuildinfo

# Lockfile policy: pnpm-lock.yaml is the source of truth.
# npm/yarn contributors are welcome but their lockfiles must not be committed.
package-lock.json
yarn.lock

# Claude Code: per-developer local state stays local; shared settings.json (no .local.) commits.
.claude/settings.local.json
.claude/projects/
.claude/sessions/
.claude/cache/
.claude/.credentials.json
